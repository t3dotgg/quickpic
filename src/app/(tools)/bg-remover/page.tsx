import { RemoveBackgroundTool } from "./remove-background-tool";

export const metadata = {
  title: "Background Remover - QuickPic",
  description: "Remove background of an image (for free because duh)",
};

export default function RemoveBackgroundToolPage() {
  return <RemoveBackgroundTool />;
}
